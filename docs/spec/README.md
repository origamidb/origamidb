# OrigamiDB specification

**Status:** Pending. The first authoritative draft has not yet been written.

This folder will hold the OrigamiDB specification — the layer-by-layer design document, with interface definitions, per-layer invariants, and the merge-algorithm and sync-protocol descriptions.

In the meantime:

- For the architectural worldview, the layer stack, and the invariants that hold across the entire design, see [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md).
- For the README's high-level description of what OrigamiDB is, see [`../../README.md`](../../README.md).

Spec contributions follow the RFC process under [`../../rfcs/`](../../rfcs/). The first authoritative spec draft is expected to land via that process.
