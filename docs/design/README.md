# OrigamiDB design

Per-layer design pages for OrigamiDB. Design intent lives here as architecture develops; once implementations land, code becomes the authoritative behavioral description and this material transitions to design history.

**Status:** Pre-implementation. Per-layer pages land as the design solidifies.

For the architectural worldview — layer stack, invariants, cross-cutting concerns — see [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md). For the README's high-level description of what OrigamiDB is, see [`../../README.md`](../../README.md).
